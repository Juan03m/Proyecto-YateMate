<form method="post" action="{{ path('app_publicacion_amarra_delete', {'id': publicacion_amarra.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ publicacion_amarra.id) }}">
    <button class="btn">Delete</button>
</form>
