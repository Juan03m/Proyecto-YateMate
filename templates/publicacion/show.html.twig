{% extends 'base.html.twig' %}

{% block title %}Publicacion{% endblock %}

{% block body %}
    <h1>Publicacion</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ publicacion.id }}</td>
            </tr>
            <tr>
                <th>Titulo</th>
                <td>{{ publicacion.titulo }}</td>
            </tr>
            <tr>
                <th>Descripcion</th>
                <td>{{ publicacion.descripcion }}</td>
            </tr>
            <tr>
                <th>Fecha</th>
                <td>{{ publicacion.fecha ? publicacion.fecha|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_publicacion_index') }}">back to list</a>

    <a href="{{ path('app_publicacion_edit', {'id': publicacion.id}) }}">edit</a>

    {{ include('publicacion/_delete_form.html.twig') }}
{% endblock %}
