{% extends 'base.html.twig' %}

{% block title %}ReservaAmarra{% endblock %}

{% block body %}
    <h1>ReservaAmarra</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ reserva_amarra.id }}</td>
            </tr>
            <tr>
                <th>Aceptada</th>
                <td>{{ reserva_amarra.aceptada ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_reserva_amarra_index') }}">back to list</a>

    <a href="{{ path('app_reserva_amarra_edit', {'id': reserva_amarra.id}) }}">edit</a>

    {{ include('reserva_amarra/_delete_form.html.twig') }}
{% endblock %}
